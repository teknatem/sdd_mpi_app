//! Страница «Инвентаризация знаний» — что в системе есть и что из этого
//! достижимо чату.
//!
//! Заменяет прежнюю страницу «База знаний»: та считала markdown-статьи, то есть
//! примерно пятую часть источников знания, и о существовании остальных четырёх
//! пятых не сообщала ничего. Дерево статей, словарь и чтение статьи никуда не
//! делись — они переехали сюда вкладками.
//!
//! Раскладка — триада detail-страницы: `api` (запросы), `view_model`
//! (состояние и команды), `page` (компонент), `tabs/` (содержимое вкладок).

pub mod api;
pub mod page;
pub mod tabs;
pub mod view_model;
