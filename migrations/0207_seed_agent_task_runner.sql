-- Регламент исполнения очереди поручений (a042) + доступ к навыку делегирования.
--
-- Задание выключено по умолчанию: каждое поручение — полный агентный прогон с
-- реальными вызовами модели, включать это следует осознанно. До включения очередь
-- исполняется кнопкой «Выполнить очередь» на странице поручений.
--
-- Кадэнс раз в 3 минуты: чаще бессмысленно — задание объявляет a018_llm_chat в
-- write_tables и всё равно стоит в очереди блокировок вместе с KB/почтой/судьёй.

INSERT OR IGNORE INTO sys_tasks (
    id, code, description, task_type, schedule_cron, config_json,
    is_enabled, created_at, updated_at, is_deleted
) VALUES (
    'a1b2c3d4-e5f6-7890-abcd-ef1234567829',
    'task029-agent-task-runner',
    'Поручения AI-сотрудникам — исполнение очереди: захват, прогон агента нужной специализации, запись результата.',
    'task029_agent_task_runner',
    '0 */3 * * * *',
    '{"max_tasks":3,"stale_minutes":45,"item_timeout_seconds":600}',
    0,
    datetime('now'),
    datetime('now'),
    0
);

-- Делегировать может координатор — он и так маршрутизатор задач по специалистам.
-- Явная строка, а не расчёт на дефолт: незаполненная ячейка матрицы означает
-- Denied для всех, кроме координатора, и намерение здесь стоит зафиксировать.
-- Специалистам навык НЕ выдаётся: при MAX_DELEGATION_DEPTH = 1 исполнитель всё
-- равно получит отказ по глубине, и лишний инструмент в наборе только сбивает.
INSERT OR IGNORE INTO sys_llm_skill_access (specialization, skill_id, access_level) VALUES
    ('coordinator_admin', 'agent-delegation', 'immediate');
